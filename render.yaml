services:
  - type: web
    name: ezeiza-actas-backend
    env: node
    region: oregon
    buildCommand: npm install
    startCommand: node server.js
    envVars:
      NODE_ENV: production
    rootDir: .
